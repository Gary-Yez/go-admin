package {{.ModuleName}}

import "gitee.com/mxcker/go-admin/server/core/models/common"

type {{.ModelName}} struct {
	{{- if .UseSoftDelete}}
	common.DbBaseModelWidthDelete
	{{- else if .UseCommon}}
	common.DbBaseModel
	{{- end}}
	{{- range .Fields}}
	{{.Key}} {{.Type}} `json:"{{if .Hidden}}-{{else}}{{.Name}}{{end}}"{{if .IndexType}} gorm:"{{.IndexType}};"{{end}}{{if .Required }} binding:"required"{{end}}`
	{{- end}}
}

