package {{.ModuleName}}

import "gitee.com/mxcker/go-admin/server/utils"

type {{.ModelName}} struct {
	{{- if .UseSoftDelete}}
	utils.DbBaseModelWidthDelete
	{{- else if .UseCommon}}
	utils.DbBaseModel
	{{- end}}
	{{- range .Fields}}
	{{.Key}} {{.Type}} `json:"{{if .Hidden}}-{{else}}{{.Name}}{{end}}"{{if .IndexType}} gorm:"{{.IndexType}};"{{end}}{{if .Required }} binding:"required"{{end}}`
	{{- end}}
}

